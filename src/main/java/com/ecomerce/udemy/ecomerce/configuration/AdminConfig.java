package com.ecomerce.udemy.ecomerce.configuration;

import java.time.LocalDate;
import java.util.List;

import com.ecomerce.udemy.ecomerce.models.data.users.Admin;
import com.ecomerce.udemy.ecomerce.models.repositories.AdminRepository;

import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class AdminConfig {
    // @Bean
    // CommandLineRunner commandLineRunner(AdminRepository adminRepository) {
    // return args -> {
    // Admin admin = new Admin();
    // admin.setPassword(admin);

    // };
    // }
}
